Run this command to access the service locally:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "platform-app.fullname" . }} 8080:{{ .Values.service.port }}

Then open http://127.0.0.1:8080/health
